const menuIcon = document.querySelector('.menu_icon');
const bottomNavbar = document.querySelector('.bottom_navbar');


menuIcon.addEventListener('click', () => bottomNavbar.classList.toggle('active'))
